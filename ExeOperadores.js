// Adição Simples: Crie uma expressão que some dois números.

let a = 1
let b = 2
let soma = a+b
console.log("a soma dos entre os números é:",soma)

// Multiplicação e Divisão: Multiplique dois números, depois divida o resultado por um terceiro número.

let c = 1
let d = 2
let num = 3
let Multiplicação = c*d
let divisão = Multiplicação/num
console.log ("O produto dos dois números é:",Multiplicação, "e a divisão do resultado é:",divisão)

// Incremento de Variável: Incremente uma variável usando o operador de incremento.

let e = 1
e++
console.log("numero:",e)

// Operador Lógico AND: Use o operador lógico AND (&&) para combinar duas condições.

let f = 1
let g = 2
if(f == 1 && g == 2){
    console.log("vc está certo")
}

// Operador Lógico OR: Use o operador lógico OR (||) para verificar se pelo menos uma de várias condições é verdadeira.

let h = 1
let i = 0
if(h == 1 || i == 2){
    console.log("vc está correto")
}